# Port the service listens on inside its container.
PORT=9200

# Nothing else is required. Authentication, rate limiting and usage metering are
# the gateway's responsibility, so no credentials for those live here — there is
# no secret in this service to leak.
